---
step: "05"
name: "Final Confirmation and Handoff"
workflow: diagnostic
agent: eclaireur
---

# Step 05 — Final Confirmation and Handoff

> **CRITICAL — Rule 4:** Blocking checkup before handoff. 100% required.
> **CRITICAL — Rule 7:** Update hk-up-status.yaml with path and diagnostic status.
> **CRITICAL — Rule 10:** Announce the explicit handoff to the next agent.
> **CRITICAL — Rule 9:** These first 10 lines are your priority.
> **CRITICAL:** Never hand off without user's explicit confirmation of project-context.md.

---

## Goal

Summarize everything. Create `project-context.md`. Final validation.
Update state. Hand off to the next agent.

---

## Step 1 — Final summary

Present a recap of all decisions made during the diagnostic:

```
Here's a summary of everything we've established:

  PROJECT
  ─────────────────────────────────────────
  Name:          {project_name}
  Type:          {project_type}
  Size:          {size_class} (~{loc_count} lines)
  Stack:         {stack_summary}
  Health:        {health_summary}
  Architecture:  {architecture_pattern}
  Git:           {project_age} · {total_commits} commits · {contributors} contributor(s)

  DECISION
  ─────────────────────────────────────────
  Objective:     {objectives_list}
  Your vision:   {user_vision_summary}
  Path:          {confirmed_path}
  Estimate:      ~{mission_count} missions

Does everything look correct? Any corrections before we proceed?
```

Wait for user confirmation. If corrections needed → apply and re-present.

---

## Step 2 — Create project-context.md

Once confirmed, write `{output_folder}/project-context.md`:

```markdown
# Project Context — {project_name}

Generated by: L'Éclaireur
Date: {date}
H.K-UP Path: {confirmed_path}

---

## Project Overview

- **Name:** {project_name}
- **Type:** {project_type}
- **Size:** {size_class} (~{loc_count} lines)

## Stack

- **Languages:** {languages}
- **Main framework:** {main_framework} {framework_version}
- **Dependencies:** {dep_count} packages

## Health Assessment

- **Tests:** {tests_status}
- **CI/CD:** {ci_status}
- **Documentation:** {readme_status}
- **Standards file:** {standards_status}

## Architecture

- **Pattern:** {architecture_pattern}
- **Notable:** {architecture_notes}

## Git History

- **Age:** {project_age}
- **Total commits:** {total_commits}
- **Contributors:** {contributors}
- **Last commit:** {last_commit_date}
- **Frequency:** {commit_frequency}

## Dependencies

{dependencies_summary}

{agent_os_section}

## Objective and Vision

- **Objective(s):** {objectives_list}
- **User's vision:** {user_vision}

## H.K-UP Path

- **Path:** {confirmed_path}
- **Estimated scope:** ~{mission_count} missions
- **Estimated effort:** ~{session_estimate} sessions

{archaeology_section}
```

> `{agent_os_section}` — include only if Agent OS was run. Header: `## Coding Standards (Agent OS)`
> `{archaeology_section}` — include only if Archéologie mode was used in step-02. Header: `## Code Archaeology`

---

## Step 3 — Additional files (Full and Audit paths only)

**If path is Full or Audit:**
- Copy `templates/gap-report.md` → `{output_folder}/gap-report.md`
- Copy `templates/debt-report.md` → `{output_folder}/debt-report.md`
- Leave placeholders unfilled — these will be completed during the diagnostic workflow

---

## Step 4 — Blocking checkup

Before handing off, verify:

| Point | Status |
|-------|--------|
| project-context.md created and confirmed by user | ✓ / ✗ |
| Objective(s) explicitly confirmed | ✓ / ✗ |
| Path explicitly confirmed | ✓ / ✗ |
| hk-up-status.yaml updated | ✓ / ✗ |

If any point is ✗ → resolve before continuing. Do not hand off.

---

## Step 5 — Update hk-up-status.yaml

Update the following fields:
- `current_phase` → appropriate phase for confirmed path
- `diagnostic` phase → `done`
- Next phase → `in-progress`
- Add confirmed path: `parcours: {confirmed_path}`

---

## Step 6 — Handoff announcement

Select the correct handoff based on confirmed path:

**Express:**
```
Diagnostic complete. Handing off to L'Architecte.

They will receive: project-context.md + your confirmed objective.
Next: architecture design and mission planning (no formal PRD).
```

**Standard / Full:**
```
Diagnostic complete. Handing off to Le Stratège.

They will receive: project-context.md + your confirmed objective + your vision.
Next: brainstorming session — choosing a method and defining the PRD.
```

**Design:**
```
Diagnostic complete. Handing off to Le Designer.

They will receive: project-context.md + UI audit data.
Next: UI/UX audit and exploration of visual directions.
```

**Audit / Light Audit / Full Audit:**
```
Diagnostic complete. Handing off to Nyx.

They will receive: project-context.md.
Next: security audit using STRIDE/DREAD methodology.
```

---

## Transition

Update `hk-up-status.yaml`:
- `3-2-objectif-parcours: done`
- `phase-3-diagnostic.status: done`
- Next phase status: `in-progress`

The diagnostic workflow is now complete.
